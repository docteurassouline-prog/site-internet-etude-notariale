# URL publique de l'outil externe de prise de rendez-vous (CLAUDE.md §2).
# Exemple : https://exemple-rendez-vous.fr/etude
NEXT_PUBLIC_BOOKING_URL=

# Endpoint HTTP recevant les soumissions du formulaire de contact (POST JSON).
# Sans valeur, /api/contact est utilisé si RESEND_API_KEY est renseignée.
# Sans transport configuré, la page propose directement le téléphone et le courriel.
#
# ATTENTION : si cet endpoint est servi par un autre domaine que le site,
# ajouter son origine à la directive connect-src de vercel.json. Sans cela
# la CSP bloque l'envoi, et l'échec est silencieux pour le visiteur.
NEXT_PUBLIC_CONTACT_ENDPOINT=/api/contact

# Clé API Resend pour l'envoi des e-mails du formulaire de contact.
# Créer un compte sur https://resend.com (100 e-mails/jour en gratuit).
# Variable serveur uniquement — jamais exposée côté client.
RESEND_API_KEY=

# Adresse e-mail destinataire des messages de contact.
# Par défaut : contact@levy-notaires.fr (depuis etude.ts).
CONTACT_DESTINATAIRE=

# Expéditeur vérifié chez Resend (ex. Site Étude Lévy <site@levy-notaires.fr>).
# Laisser vide conserve l’expéditeur de test Resend, limité par le prestataire.
# À configurer avec un domaine vérifié avant d’ouvrir l’envoi à tous les visiteurs.
CONTACT_EXPEDITEUR=

# URL du service de paiement en ligne (CLAUDE.md §13, arbitrage n° 3).
# Vide tant qu'aucun prestataire n'est retenu et qu'aucun circuit
# d'encaissement n'est validé par le comptable taxateur : l'entrée
# « Paiement en ligne » de l'accueil n'apparaît pas. Périmètre : sommes dues
# à l'étude (émoluments, honoraires, débours, taxes afférentes) — jamais de
# fonds de dossier.
NEXT_PUBLIC_PAIEMENT_URL=

# URL canonique du site (sans barre oblique finale) — flux RSS, sitemap,
# canonicals et données structurées. Défaut en production : https://levy-notaires.fr
# (src/config/site.ts). Ne renseigner ici qu'en préproduction ou en local.
NEXT_PUBLIC_SITE_URL=
